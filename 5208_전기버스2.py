test_num = int(input())

for t in range(test_num):

    base_list = list(map(int,input().split()))
    N = base_list.pop(0)
    min_cnt = 99999

    def refill(arr, k, cnt):
        global min_cnt
        if k >= N:
            if min_cnt > cnt
            break

    print(N, base_list)
    print('#' + str(t+1) + ' ', end='')